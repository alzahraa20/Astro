<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.zahra.astro">

    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activities.InteractActivity"></activity>
        <activity android:name=".lessons.SpaceVideoActivity" />
        <activity android:name=".lessons.EarthQuizActivity" />
        <activity android:name=".activities.MapActivity" />

        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/map_key" />

        <activity android:name=".activities.SolarWeightActivity" />
        <activity android:name=".activities.SolarAgeActivity" />
        <activity android:name=".home.ActivitiesOptActivity" />
        <activity android:name=".lessons.SpaceQuizActivity" />
        <activity android:name=".lessons.EarthVideoActivity" />
        <activity android:name=".lessons.SpaceLessonActivity" />
        <activity android:name=".lessons.EarthLessonActivity" />
        <activity android:name=".home.SplashSpaceActivity" />
        <activity android:name=".topics.SpaceTopicsActivity" />
        <activity android:name=".topics.EarthTopicsActivity" />
        <activity android:name=".home.SplashEarthActivity" />
        <activity android:name=".home.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>